/* ------------------------------------------------------------------
   Single source of truth for the site's public URL.
   Used by metadata (OG / Twitter / canonical), JSON-LD, sitemap, robots.

   Set to the final nishalcr.is-a.dev custom domain, so SEO metadata matches
   the moment the domain goes live (GitHub Pages). See DEPLOY.md. If the public
   URL ever changes, edit this ONE line.
   ------------------------------------------------------------------ */
export const SITE_URL = "https://nishalcr.is-a.dev";

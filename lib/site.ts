/* ------------------------------------------------------------------
   Single source of truth for the site's public URL.
   Used by metadata (OG / Twitter / canonical), JSON-LD, sitemap, robots.

   The primary URL is the nishalcr.is-a.dev custom domain (registered via
   is-a-dev/register), served by GitHub Pages. If the domain ever changes,
   update this ONE line. See DEPLOY.md.
   ------------------------------------------------------------------ */
export const SITE_URL = "https://nishalcr.is-a.dev";

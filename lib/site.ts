/* ------------------------------------------------------------------
   Single source of truth for the site's public URL.
   Used by metadata (OG / Twitter / canonical), JSON-LD, sitemap, robots.

   The live URL is the GitHub Pages user site. If you later attach the
   nishalcr.is-a.dev custom domain, change this ONE line to it. See DEPLOY.md.
   ------------------------------------------------------------------ */
export const SITE_URL = "https://nishalcr.github.io";
